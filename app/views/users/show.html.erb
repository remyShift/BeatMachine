
<div class="ml-4 mt-6 flex justify-between w-full">
  <div class="flex flex-col gap-12">
    <h2 class="text-3xl font-bold font-kaushan text-primary">Your profile</h2>
    <div class="flex flex-col mt-4 gap-4">
      <%= cl_image_tag @user.profile_picture.key, class: "w-40 h-40 rounded-full object-cover border-2 border-primary" %>
    </div>
  </div>

  <div class="flex flex-col gap-12">
    <h2 class="text-3xl font-bold font-kaushan text-primary">Your drumracks</h2>
    <div class="flex flex-col mt-4 gap-4">
      <% @drumracks.each do |drumrack| %>
        <%= render "shared/music_card", drumrack: drumrack, display_like_button: false %>
      <% end %>
    </div>
  </div>

    <div class="w-3/4 flex flex-col mr-12 gap-4 shadow-lg shadow-black-500/50 p-2 md:w-1/2 bg-white rounded-lg mb-4 md:mb-0 overflow-y-auto max-h-[270px]">
>>>>>>> origin/main
  <% if @likes.present? %>
    <h3 class="text-3xl font-bold font-kaushan flex justify-center text-white">Your likes</h3>
    <ul class="flex items-center justify-center flex-wrap gap-4">
      <% @likes.each do |like| %>
        <%= render "shared/music_card", drumrack: like.drumrack, display_like_button: false, class: "bg-gradient-to-br from-customforestGreen/70 to-customforestGreen/30" %>
      <% end %>
    </ul>
  <% else %>
    <h3 class="text-3xl font-bold text-white">Your likes</h3>
    <p class="text-lg">You have no drumracks liked yet.</p>
    <%= link_to "Browse drumracks", drumracks_path, class: "text-blue-500 underline" %>
  <% end %>
    </div>
    <div class="w-3/4 flex flex-col mt-8 gap-4 shadow-lg shadow-black-500/50 p-2 md:w-1/2 rounded-lg mb-4 md:mb-0 overflow-y-auto max-h-[270px]" style="scrollbar-width: none; -ms-overflow-style: none;">
  <% if @likes.present? %>
    <h3 class="text-3xl font-bold font-kaushan flex justify-center text-white">Your drumracks</h3>
    <ul class="flex items-center justify-center flex-wrap gap-4">
      <% @likes.each do |like| %>
          <%= render "shared/music_card", drumrack: like.drumrack, display_like_button: false %>
        <% end %>
      </ul>
    <% else %>
      <h3 class="text-3xl font-bold text-primary">Your likes</h3>
      <p class="text-lg">You have no drumracks liked yet.</p>
      <%= link_to "Browse drumracks", drumracks_path, class: "text-blue-500 underline" %>
    <% end %>
    <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete } %>
  </div>
>>>>>>> origin/main

</div>

<%= render "shared/menubar" %>
