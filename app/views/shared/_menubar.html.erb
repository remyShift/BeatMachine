<nav class="w-full bg-gradient-to-b from-greyy to-blackk px-2 pb-10 pt-4 flex justify-evenly items-center shadow-top fixed bottom-0 z-50 border-t-2 border-yelloww"
    data-controller="menubar"
>

    <%= link_to templates_drumracks_path, class: "flex flex-col items-center", data: {
        menubar_target: "toToggle",
        action: "click->menubar#toggle",
        turbo: "false"
    } do %>
        <i class="fa-solid fa-music text-xl text-yelloww cursor-pointer"
        ></i>
        <div class="w-6 h-6 bg-white rounded-full absolute bottom-0 blur-lg hidden"
        ></div>
    <% end %>

    <%= link_to root_path, class: "flex flex-col items-center", data: {
        menubar_target: "toToggle",
        action: "click->menubar#toggle",
        turbo: "false"
    } do %>
        <i class="fa-solid fa-house text-xl text-yelloww"
        ></i>
        <div class="w-6 h-6 bg-white rounded-full absolute bottom-0 blur-lg hidden"
        ></div>
    <% end %>

    <%= link_to current_user.present? ? user_path(current_user) : new_user_session_path, class: "flex flex-col items-center", data: {
        menubar_target: "toToggle",
        action: "click->menubar#toggle",
        turbo: "false"
    } do %>
        <i class="fa-solid fa-user text-xl text-yelloww"
        ></i>
        <div class="w-6 h-6 bg-white rounded-full absolute bottom-0 blur-lg hidden"
        ></div>
    <% end %>
</nav>
